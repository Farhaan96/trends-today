# 2026-07-31 18:30 Coquitlam and Whitecaps release plan

## Review reason and scope

This is material-plan review cycle 1 of 2 for two fresh, low-risk editorial lanes: a new Coquitlam fall-recreation registration update and a same-route Whitecaps Aug. 1 matchday refresh. The review is required before candidate drafting, image generation, promotion, or public mutation. Fable is not used.

Opus 5 reviewed checkpoint `a4afb4789ee26561de4e3e6af1bce9005e7b2eee` and returned `BLOCKERS`. This amended baseline incorporates every blocker and executable delta without spending a second plan review. The remaining Opus cycle is reserved for the finished exact release SHA. A blocker at that release review means stop-and-report; there is no automatic repair review budget.

Final scope amendment before Opus cycle 2: Whitecaps remains editorially qualified but is terminally held after five exact GPT blocker artifacts. Its last candidate hash is `ddc76fbfed1876892c7e71664e4d169d031abdbfbac9db43325213705da5df86`; it will not be promoted. Only the Coquitlam candidate may proceed after a fresh GPT PASS at the final evidence SHA. Opus cycle 2 must review this scope reduction and the exact Coquitlam release artifact in one structured call.

The exact base is `2f9bf3b7ddca8a56218eea271fe2cfddec2e6763` on `codex/lm-daily-2026-07-31-1840`. The dirty root checkout remains untouched at `8ffbb445c1d9521ba72cd12362bb2994b89df2a1`. The failed exFAT checkout at `D:\CodexWorktrees\trends-daily-2026-07-31-1834` is preserved without repair, reset, clean, removal, or reuse. Linear issue `COS-843` records the lane.

## Evidence and unknowns

- Public metrics at `2026-08-01T01:38:32.237Z`: 169 active articles and zero July 31 publications. Protected reporting returned HTTP 401. Vercel, GA4, Search Console, ad, sponsor, revenue, conversion, and cost data are unavailable, never zero.
- All 14 enabled official listings returned HTTP 200 and produced 56 opportunities. Versus the 08:30 queue: eight URLs added, seven removed, zero retained titles changed. Canonical URL/title SHA-256: `f520bca07b42e5a19a5cc9ebd272db8aba5f32bd26df7fa177496e54266e0534`.
- Coquitlam facts are pinned to four official URLs with exact response hashes in `2026-07-31-1830-targeted-source-extracts.json`. The official registration page confirms the four remaining Pro-D/general dates and the 8:30 a.m. opening time, while also showing the July swimming and skating phases that prevent an overbroad "registration starts Aug. 4" claim.
- Whitecaps discovery evidence includes the July 31 spectator page, but the exact release source set is narrowed to the accessible BC Place event page plus the official Whitecaps schedule after the GPT route could not resolve the spectator URL. BC Place supports Aug. 1, 4:30 p.m., 3 p.m. gates, all published ticket-entry methods, bag constraints and cashless facts; the schedule corroborates the match route. Promotional crowd figures, 50/50 material, sponsor activations and personal/medical details are excluded.
- The current Whitecaps article already covers the same match and route, but its title and body are dominated by promotional attendance claims. The plan refreshes that existing URL rather than publishing a duplicate.
- The existing original Whitecaps stadium illustration was visually inspected and remains subject-matched. Hash: `2a2eb3ad3f0e4fa676712f5b6c157e84de46ce518b4dceca6a569076a07bb4c3`.
- Known route limitation: the live category route renders the article headline as hero alt and does not visibly render `imageAttribution`. Browser proof must record rendered alt verbatim and may verify provenance only against committed frontmatter/artifacts, not claim visible attribution.

## Implementation plan

1. Re-fetch the public daily count and all seven material source URLs immediately before drafting. Stop on HTTP failure, changed dates/times, source conflict, or response-hash drift that changes a material claim.
2. Stage exactly two candidates outside `content/`, but promote only a candidate that clears GPT and Opus. Whitecaps is preserved as review-blocked evidence; Coquitlam is the sole final release lane:
   - `artifacts/editorial/release-candidates/local-news/coquitlam-fall-recreation-registration-august-2026.mdx`
   - `artifacts/editorial/release-candidates/sports/whitecaps-30000-tickets-bc-place-return-lafc.mdx`
3. Coquitlam candidate: 550-750 useful editorial words while the mechanical `len(body.split())` count must remain within the configured 550-950 range; 3-5 highlights; at least three H2s; at least three enumerable lists; four official source anchors; zero em dashes across the full candidate bytes; no invented fee/availability/eligibility claim; and no private-data advice beyond the City's public account-preparation guidance. Deliver the four remaining opening dates, the earlier July swimming/skating phases, 8:30 a.m. time, online/in-person/phone paths, account checklist, program-guide scope, and a concise assistance note.
4. Whitecaps refresh: preserve the legacy slug and original `publishedAt`, add a truthful `modifiedAt`, and rewrite title, subtitle, description, imageAlt, readerImpact, all highlights, reportingMethod, and body from promotional attendance framing to matchday service utility. Drop stale `archiveReviewStatus` and `archiveReviewNote`. The new `reportingMethod` must name the July 31, 2026 BC Place and official schedule re-check and disclose exclusion of the inaccessible spectator URL. Deliver kickoff, gate time, early-arrival reason, the venue-supported Mobile Wallet tap plus app/physical barcode entry methods, no-in-and-out rule, bag dimensions, cashless policy, and stadium-district access warning. Omit Gate B and supporter-section claims that the GPT route could not independently reopen. Cite BC Place and official schedule URLs verbatim in the body. Omit every attendance or tickets-sold figure, every 50/50 fact or link, sponsor activation, unsupported transit guidance, speculative traffic prediction, and personal/medical detail.
5. Preserve required fields on both candidates: `lengthRationale` with a story-specific rationale for the `reported-update` contract; `commercialIntent: none`; truthful `commercialFitReason`; `brandSafety: standard`; `sponsorshipStatus: editorial`; `commercialApprovalRecorded: false`; `manualApprovalRequired: false`; and `manualApprovalRecorded: false`. Commercial fit does not affect selection.
6. Generate one original Coquitlam hero with the built-in image-generation route. Use a photorealistic-natural editorial scene: an unbranded community recreation sign-up desk with a laptop, printed activity guide, swim goggles, and a basketball in a bright civic recreation lobby; no people, logos, readable text, watermark, payment card, government seal, or copied City imagery. Save non-destructively at `public/images/editorial/2026/07/coquitlam-fall-recreation-registration.png`. Record final prompt, tool route, output ID/path, dimensions, byte count, SHA-256, visual inspection, alt text, and truthful AI attribution in `artifacts/editorial/images/2026-07-31-1830-coquitlam-registration-image-provenance.json`.
7. Reuse the inspected Whitecaps illustration without modification. Preserve its existing original-editorial attribution and record its exact hash in the run evidence. Do not create a second Whitecaps image.
8. Run `validate_release_candidate` independently for each artifact and supplement it with mechanical assertions: source count, primary source, locality, reader impact, story type, `lengthRationale`, `len(body.split())` word range, H2/list/highlight counts, paragraph limits, required commercial fields, crawlable source anchors, published internal links only, unique SEO title/description, and utility-promise handling. If `where|location|locations|places|centres|centers` occurs in title/subtitle/readerImpact, require a matching `where|find|location|locations|places` H2 and at least three list items. Scan the full candidate bytes against all 23 configured `manualApprovalKeywords`, not a hand-picked subset, and require zero hits. Run the repository em-dash counter on the full candidate text including frontmatter and require zero.
9. Commit the exact candidates, image/provenance, deterministic evidence, and refreshed source evidence, but not either review artifact, as the release evidence checkpoint. Require clean status and record `REVIEW_SHA`. This avoids the impossible self-referential requirement of committing a review artifact whose `repositorySha` must equal the commit containing it.
10. Run the exact Coquitlam candidate through the GPT editorial gate while `HEAD == REVIEW_SHA`. Preserve the resulting artifact uncommitted at exactly `artifacts/editorial/reviews/gpt/local-news/<file>.json` until promotion. It must be UTF-8 without BOM and contain every required field: `version: 1`, `reviewer: openai-gpt`, `verdict: PASS`, exact `candidateSha256`, `reviewedAt`, exact `repositorySha`, supported `reviewBackend`, `reviewRunId`, GPT model ID, all five integer scores at 4 or 5, `blockers: []`, and `proseEmDashCount: 0`.
11. Run the exact Whitecaps candidate through the same GPT gate at exactly `artifacts/editorial/reviews/gpt/sports/<file>.json`, with a separate exact candidate hash and verifier artifact. Repair valid GPT blockers before the final Opus call, recreate `REVIEW_SHA`, and rerun both GPT reviews after any candidate-byte change.
12. Create a separate clean detached worktree at the exact `REVIEW_SHA` and run `C:/Users/farha/.codex/scripts/invoke-claude-review.ps1` there using `-PrimaryModel claude-opus-5 -DisableFallback`. The prompt must require a structured verdict for both exact candidates, factual/source mapping, duplicate/replace-existing safety, images/provenance, SEO/canonical/structured-data truth, deterministic checks, promotion sequencing, release scope, and owner boundaries. The detached worktree must remain clean; the GPT artifacts stay uncommitted in the issue worktree and their accepted schema/hash/SHA results are supplied to Opus in the evidence packet without pretending they are committed files.
13. Transform the non-empty runner result into separate verifier-shaped UTF-8-without-BOM artifacts in the issue worktree. Each must contain `version: 1`, `reviewer: claude`, `verdict: NO BLOCKERS`, exact `candidateSha256`, `reviewedAt`, runner `reviewedSha` as `repositorySha`, `modelUsed: claude-opus-5`, observed models, fallback flags, runner status, and runner output path. Keep these artifacts uncommitted through promotion. Verify first byte `0x7B`, no `EF BB BF` prefix, and exact binding with the repository verifiers.
14. The final exact-SHA Opus call is cycle 2 of 2 for this unchanged task scope. Any candidate-byte repair before that call repeats deterministic validation, creates a new release evidence checkpoint, and reruns both GPT reviews. Any blocker or byte-repair need after the final Opus call means stop and report the blocker; do not start a third Opus review without Farhaan's explicit approval plus a material scope reset, new evidence, or genuine new risk.
15. At the exact reviewed SHA, run the full 87-test Python pipeline, JSON parsing, image inspection/dimensions/hash, scoped Prettier, `git diff --check`, `npm ci --no-audit --no-fund`, `npm run typecheck`, quiet zero-error lint, and production build. Record inherited repository-wide findings separately; do not waive candidate failures.
16. Promote only the exact-reviewed Coquitlam candidate while `HEAD == REVIEW_SHA` and before committing its GPT or Claude review artifact, because the verifiers require live HEAD to equal `repositorySha`. Do not invoke `--replace-existing`; Whitecaps remains held. Assert that promotion preserves the reviewed Coquitlam bytes and that the public destination path is exact.
17. Commit the final Coquitlam GPT and Claude artifacts, the promoted Coquitlam article, every blocked GPT artifact, the held Whitecaps candidate, image/provenance, deterministic evidence, and dated learning entry together so the promoted frontmatter review paths resolve in the final commit. Push `codex/lm-daily-2026-07-31-1840`, open one PR linked to `COS-843`, add `codex` and `codex-automation`, require all checks, and merge without deleting the branch. Compare current `origin/main`, never the stale local `main`, and stop on unexpected diff, check failure, review mismatch, or remote movement that cannot be reconciled safely.
18. Wait for the exact merge SHA's Production deployment to succeed. Record deployment ID, URL, timestamps, and rollback point `2f9bf3b7ddca8a56218eea271fe2cfddec2e6763`.
19. Browser-verify homepage and the exact Coquitlam production route: HTTP 200, canonical, rendered headline/body, dates, source/internal links, loaded hero with non-zero dimensions, Article/Breadcrumb structured data truth, zero page/console errors, zero HTTP 4xx/5xx, and no non-analytics failed requests. Confirm the existing Whitecaps route was not changed by this sweep. Record `renderedAlt` verbatim and the known attribution-route limitation; do not claim visible image attribution.
20. Write the dated learning entry with run ID, trigger, SHAs, sources, all qualification/rejection/skip reasons, model IDs, exact review artifacts, tests, PR, merge, deployment, browser proof, rollback, cost status, metric contract, and keep/repair/stop rule. Update `COS-843` only at material gates.

## Acceptance tests

- Exactly two candidate artifacts exist and no `content/` file changes before promotion.
- The Coquitlam candidate identifies the four August dates as the remaining Pro-D/general openings, discloses the earlier July swimming/skating phases, presents 8:30 a.m. in a scannable section, and maps every material claim to an exact source extract.
- The Whitecaps candidate retains the existing legacy slug and published date, has a later truthful modified date, and contains no attendance figure (`30,000`, `30000`, `35,000`, `20,000`, `25,000`), `50/50`, jackpot, odds, sponsor activation, or personal/medical detail in title, subtitle, description, imageAlt, readerImpact, highlights, or body. The `slug:` line is the sole permitted `30000` occurrence: replace-existing enforces slug equality, while a new slug would fork the canonical route.
- Both candidates contain exactly one non-empty, story-specific `lengthRationale`. The held Whitecaps `reportingMethod` names July 31, 2026, the two re-checked release URLs and the excluded inaccessible spectator URL; neither stale archive-review field is present.
- Every source URL passed to validation appears verbatim in the candidate body. All four Whitecaps highlights are new, unique and free of attendance framing.
- The held Whitecaps candidate contains `3 p.m.`, `4:30 p.m.`, both bag dimensions, venue-supported tap/barcode guidance, cashless guidance and the stadium-district access warning, but its latest exact GPT artifact remains blocked and therefore prevents promotion.
- Both candidates pass `validate_release_candidate`, the configured reported-update contract, full-source-link check, SEO/canonical expectations, whole-file scan against all 23 manual-approval keywords, full-file zero-em-dash gate, mechanical 550-950 body-token range, and utility-promise trap.
- The Coquitlam image is original, text-free, unbranded, subject-matched, visually inspected, and exactly bound to its provenance hash. The Whitecaps image hash remains unchanged.
- The final Coquitlam GPT artifact and sole Opus verifier bind the exact Coquitlam candidate hash and exact repository SHA; verifier first byte is `{` and no BOM is present. All five blocked Whitecaps GPT artifacts remain preserved and none is treated as release authorization.
- `git rev-parse HEAD` equals `REVIEW_SHA` immediately before promotion; review-artifact commits happen after promotion.
- PR checks, exact merge deployment, and production browser assertions all pass before Coquitlam is reported browser-verified. Whitecaps is explicitly reported as not implemented, merged, deployed or browser-verified by this sweep.
- After promotion, the published Coquitlam `editorialReviewArtifact` and `reviewArtifact` paths resolve to files committed in the final release commit.

## Authorization boundaries and stop conditions

Routine qualified editorial publication, merge, deployment, and live verification are authorized after every gate passes. This plan does not authorize advertiser or sponsor replies, supported/branded coverage, pricing, terms, guarantees, billing, provider/DNS/secret changes, production-data mutations, private/customer data use, regulated-gambling coverage, or any sensitive story. The commercial inbox remains fail-closed.

Stop on source conflict, material hash drift, uncertain fact, private-person risk, unsupported claim, duplicate-route ambiguity, unexpected image content, candidate/review mismatch, malformed or empty reviewer output, failed checks, dirty-scope ambiguity, main movement, deployment ambiguity, or unverifiable live state.
